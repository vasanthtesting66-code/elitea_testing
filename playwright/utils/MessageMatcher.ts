/**\n * MessageMatcher – utility to match actual UI messages against\n * a list of acceptable expected message variants.\n *\n * Accounts for different wording across application versions / locales.\n */\nexport class MessageMatcher {\n  /**\n   * Returns true if the actual message contains at least one\n   * of the expected substrings (case-insensitive).\n   *\n   * @param actual    - Text content retrieved from the UI\n   * @param expected  - Array of acceptable message variants\n   */\n  static containsAny(actual: string, expected: string[]): boolean {\n    const normalised = actual.toLowerCase().trim();\n    return expected.some(variant => normalised.includes(variant.toLowerCase().trim()));\n  }\n\n  /**\n   * Build a human-readable assertion failure message listing all accepted variants.\n   */\n  static buildMismatchMessage(actual: string, expected: string[]): string {\n    return (\n