import { APIRequestContext, request } from '@playwright/test';\n\nexport class HttpHelper {\n  /**\n   * Performs a GET request to the provided URL and returns the HTTP status code.\n   * Uses a standalone APIRequestContext so it can be used without a browser page.\n   */\n  static async getStatusCode(url: string): Promise<number> {\n    const context: APIRequestContext = await request.newContext();\n    try {\n      const response = await context.get(url);\n      return response.status();\n    } finally {\n      await context.dispose();\n    }\n  }\n}\n