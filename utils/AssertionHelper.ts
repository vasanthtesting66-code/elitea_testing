import { Page, expect } from '@playwright/test';\nimport { config } from '../config/config';\n\nexport class AssertionHelper {\n  /**\n   * Assert the current page URL contains the expected path fragment\n   */\n  static async assertUrlContains(page: Page, expectedPath: string): Promise<void> {\n    await expect(page).toHaveURL(new RegExp(expectedPath.replace(/\\//g, '\\\\/')));\n  }\n\n  /**\n   * Assert the browser tab title contains the expected string\n   */\n  static async assertTitleContains(page: Page, expectedTitle: string): Promise<void> {\n