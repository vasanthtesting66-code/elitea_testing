import { test as base, Page } from '@playwright/test';\\nimport { LoginPage } from '../pages/LoginPage';\\nimport { DashboardPage } from '../pages/DashboardPage';\\nimport { config } from '../config/config';\\n\\n/**\\n * Extended fixture types for authentication-related tests\\n */\\nexport type AuthFixtures = {\\n  loginPage: LoginPage;\\n  dashboardPage: DashboardPage;\\n  authenticatedPage: Page;\\n};\\n\\n/**\\n * Base fixture – provides LoginPage and DashboardPage instances\\n * with the browser already positioned at the login page.\\n */\\nexport const test = base.extend<AuthFixtures>({\\n  loginPage: async ({ page }, use) => {\\n    const loginPage = new LoginPage(page);\\n    await loginPage.goto();\\n    await use(loginPage);\\n  },\\n\\n  dashboardPage: async ({ page }, use) => {\\n    const dashboardPage = new DashboardPage(page);\\n    await use(dashboardPage);\\n  },\\n\\n  /**\\n   * Provides a page that is already authenticated.\\n   * Re-uses this fixture when the test only needs to verify post-login state.\\n   */\\n  authenticatedPage: async ({ page }, use) => {\\n    const loginPage = new LoginPage(page);\\n    await loginPage.goto();\\n    await loginPage.login(\\n      config.credentials.admin.username,\\n      config.credentials.admin.password\\n    );\\n    await page.waitForURL(`**${config.routes.dashboard}**`, {\\n      timeout: config.timeouts.navigation,\\n    });\\n    await use(page);\\n  },\\n});\\n\\nexport { expect } from '@playwright/test';\\n